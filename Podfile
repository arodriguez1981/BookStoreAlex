# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'BookStoreAlexander' do

  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'RealmSwift'
  pod 'AFNetworking'
  pod 'SwiftyJSON'
  pod 'Alamofire'
  pod 'SwiftFontIcons', :git => 'https://github.com/daaavid/SwiftFontIcons.git', :branch => 'master'
   
  # Pods for BookStoreAlexander

  target 'BookStoreAlexanderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BookStoreAlexanderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

#post_install do |installer_representation|
 #   installer_representation.pods_project.targets.each do |target|
  #      target.build_configurations.each do |config|
   #         config.build_settings['SWIFT_VERSION'] = '4'
   #     end
   # end
#end


